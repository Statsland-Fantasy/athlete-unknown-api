# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Example for production:
ALLOWED_ORIGINS=https://app.statslandfantasy.com,https://statslandfantasy.com

# Example for development (these are the defaults if not set in dev mode):
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Auth0 Configuration
AUTH0_DOMAIN=your-domain.auth0.com
AUTH0_AUDIENCE=your-api-audience

# Admin API Key
ADMIN_API_KEY=your-secure-api-key-here

# DynamoDB Configuration
# Leave DYNAMODB_ENDPOINT empty for production (uses AWS)
# Set to http://localhost:8000 for local development
DYNAMODB_ENDPOINT=localhost:8000
ROUNDS_TABLE_NAME=AthleteUnknownRounds
USER_STATS_TABLE_NAME=AthleteUnknownUserStats
AWS_REGION=us-west-2

# Server Configuration
PORT=8080
GIN_MODE=release

# AI Image Upscaler Configuration
# Enable/disable AI-powered photo upscaling
AI_UPSCALER_ENABLED=false
# API key for your upscaling service (e.g., Replicate, DeepAI, Let's Enhance)
AI_UPSCALER_API_KEY=your-upscaler-api-key-here
# API endpoint URL for the upscaling service
AI_UPSCALER_API_URL=https://api.upscaler-service.com/v1/upscale
